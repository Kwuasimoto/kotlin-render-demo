logging:
  level:
    org:
      springframework:
        security: DEBUG

spring:
  profiles:
    active: default
  jpa:
    open-in-view: false

hibernate:
  jdbc:
    lob:
      non_contextual_creation: true
